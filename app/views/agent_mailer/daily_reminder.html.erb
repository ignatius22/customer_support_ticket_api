<h1>Hello <%= @agent.email %>,</h1>

<p>You have <strong><%= @tickets.count %></strong> open ticket(s) assigned to you.</p>

<ul>
  <% @tickets.each do |ticket| %>
    <li><strong><%= ticket.title %></strong> — <%= ticket.description.truncate(50) %></li>
  <% end %>
</ul>

<p>Thanks,<br>The Support Team</p>
